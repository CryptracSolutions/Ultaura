# =============================================================================
# ULTAURA ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.local and fill in the values
# These variables are REQUIRED for Ultaura voice companion functionality
# =============================================================================

# -----------------------------------------------------------------------------
# TWILIO CONFIGURATION (Required)
# Get these from https://console.twilio.com
# -----------------------------------------------------------------------------
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token_here

# Twilio phone number in E.164 format (e.g., +14155551234)
# This is the number Ultaura will call from
TWILIO_PHONE_NUMBER=+1XXXXXXXXXX

# Twilio Verify Service SID for phone verification
# Create one at: Console > Verify > Services
TWILIO_VERIFY_SERVICE_SID=VAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# XAI GROK VOICE AGENT (Required)
# Get your API key from https://console.x.ai
# -----------------------------------------------------------------------------
XAI_API_KEY=xai-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Grok Voice Agent WebSocket URL
XAI_REALTIME_URL=wss://api.x.ai/v1/realtime

# Grok model to use for voice conversations
XAI_GROK_MODEL=grok-3-fast

# -----------------------------------------------------------------------------
# TELEPHONY BACKEND (Required)
# Configuration for the Ultaura telephony server
# -----------------------------------------------------------------------------

# Port for the telephony server (default: 3001)
TELEPHONY_PORT=3001

# Public URL where the telephony server is accessible
# Used for Twilio webhooks - must be publicly accessible
# In development, use ngrok: ngrok http 3001
TELEPHONY_PUBLIC_URL=https://your-telephony-server.com

# Internal URL for telephony server (for Next.js server actions)
TELEPHONY_INTERNAL_URL=http://localhost:3001

# JWT secret for internal API authentication between Next.js and telephony
ULTAURA_INTERNAL_API_SECRET=generate_a_secure_random_string_here

# -----------------------------------------------------------------------------
# ENCRYPTION (Required)
# AES-256-GCM encryption for memory storage
# -----------------------------------------------------------------------------

# Key Encryption Key (KEK) - 32 bytes, base64 encoded
# Generate with: openssl rand -base64 32
ULTAURA_KEK_BASE64=generate_with_openssl_rand_base64_32

# -----------------------------------------------------------------------------
# STRIPE PRICING IDS (Required for billing)
# Create these products/prices in Stripe Dashboard
# -----------------------------------------------------------------------------

# Monthly subscription prices
STRIPE_ULTAURA_CARE_MONTHLY_PRICE_ID=price_xxxxxxxxxxxxxxxx
STRIPE_ULTAURA_COMFORT_MONTHLY_PRICE_ID=price_xxxxxxxxxxxxxxxx
STRIPE_ULTAURA_FAMILY_MONTHLY_PRICE_ID=price_xxxxxxxxxxxxxxxx

# Annual subscription prices
STRIPE_ULTAURA_CARE_ANNUAL_PRICE_ID=price_xxxxxxxxxxxxxxxx
STRIPE_ULTAURA_COMFORT_ANNUAL_PRICE_ID=price_xxxxxxxxxxxxxxxx
STRIPE_ULTAURA_FAMILY_ANNUAL_PRICE_ID=price_xxxxxxxxxxxxxxxx

# Pay-as-you-go metered price (per minute)
STRIPE_ULTAURA_PAYG_PRICE_ID=price_xxxxxxxxxxxxxxxx

# Overage price (metered, per minute over included)
STRIPE_ULTAURA_OVERAGE_PRICE_ID=price_xxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# OPTIONAL CONFIGURATION
# -----------------------------------------------------------------------------

# Enable debug logging for telephony
TELEPHONY_DEBUG=false

# Maximum call duration in seconds (default: 1800 = 30 minutes)
MAX_CALL_DURATION_SECONDS=1800

# Default timezone for new lines
DEFAULT_TIMEZONE=America/Los_Angeles

# Enable call recording (requires additional Twilio setup)
ENABLE_CALL_RECORDING=false

# Twilio status callback URL (defaults to TELEPHONY_PUBLIC_URL/twilio/status)
# TWILIO_STATUS_CALLBACK_URL=

# -----------------------------------------------------------------------------
# DEVELOPMENT ONLY
# -----------------------------------------------------------------------------

# Skip phone verification in development (NOT for production!)
# SKIP_PHONE_VERIFICATION=false

# Use Twilio test credentials
# TWILIO_TEST_MODE=false
